version: "3.8"

services:
  db:
    container_name: classicmodels-db
    image: postgres:15
    env_file:
      - .env
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - ./seed.sql:/docker-entrypoint-initdb.d/seed.sql
      - pg_data:/var/lib/postgresql/data
volumes:
  pg_data: